package proba;

import java.util.LinkedList;
/*
import pl.edu.agh.kis.restaurant.Cuisine;
import pl.edu.agh.kis.restaurant.Product;
import pl.edu.agh.kis.restaurant.TypeOfProduct;*/

public class Proba {
	public static void main(String[] args) {
		/*TypeOfProduct typ1 = TypeOfProduct.ADDITION;
		TypeOfProduct typ2 = TypeOfProduct.ADDITION;
		TypeOfProduct typ3 = TypeOfProduct.DESSERT;

		if ((typ1 == typ2) && (typ1 != typ3)) {
			System.out.println("jakos dziala");
		}
		
		Cuisine kuch1 = new Cuisine("polska");
		Cuisine kuch2 = new Cuisine("niemiecka");
		kuch1.addProduct(TypeOfProduct.ADDITION, "lemon", 2.5);
		System.out.println(kuch1.getAllProductsOfType(TypeOfProduct.ADDITION).size());
		System.out.println(kuch2.getAllProductsOfType(TypeOfProduct.ADDITION).size());
		//powyzej dowod na ...
		
		LinkedList<Product> l = new LinkedList<Product>();
		Product p  = new Product(TypeOfProduct.ADDITION,"kawa",2.5);
		l.add(p);
		//p.name = "alibaba";   //to zostalo zmienione bo ta zmienna musi byc prywatna
		System.out.println(l.get(0).getName());
		//powyzej dowod na to ze mozna zmieniac obiekty na liscie jesli mamy do nich referencje (logiczne)
		
		for(Product kawa : l) {
			if(kawa == p)
				System.out.println("bomba");
		}*/
	}
}
